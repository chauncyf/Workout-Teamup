<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand mr-4" href="/">
    <i class="fas fa-dumbbell"></i>
    WORKOUT TEAMUP</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-start" id="navbarSupportedContent">
    <!--    float-right-->
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "Discover", root_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <% if !logged_in? %>
          <%= link_to "Achievement", login_path, {remote: true, 'data-toggle': "modal", 'data-target': "#login", class: "nav-link"}%>
        <% else %>
          <a class="nav-link" href=<%= joined_activities_path %>>Achievement</a>
        <% end %>
      </li>
      <li class="nav-item">
        <% if !logged_in? %>
          <%= link_to "Map", login_path, {remote: true, 'data-toggle': "modal", 'data-target': "#login", class: "nav-link"}%>
        <% else %>
          <%= link_to "Map", :map_path, class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if !logged_in? %>
          <%= link_to "Chat", login_path, {remote: true, 'data-toggle': "modal", 'data-target': "#login", class: "nav-link"}%>
        <% else %>
          <a class="nav-link" href="#">Chat</a>
      <% end %>
      </li>

      <li class="divider-vertical"></li>

      <% if logged_in? %>
<!--        <li class="nav-item line-height: 50px"><%#= avatar_for current_user %></li>-->
        <li class="nav-item dropdown">
<!--          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Me</a>-->
          <a class="nav-link p-0" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= avatar_for current_user %></a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
<!--          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">-->
            <p class="dropdown-item mb-0">Hi, <%= current_user.user_name %></p>
            <p class="dropdown-divider"></p>
            <a class="dropdown-item" href=<%= user_path(current_user) %>>Profile</a>
            <a class="dropdown-item" href="#">My Poster</a>
            <a class="dropdown-item" href="#">Analysis</a>
            <p class="dropdown-divider"></p>
            <%= link_to "Logout", logout_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>

        <% if current_user.identity == 1 %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">ControlPanel</a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
<!--            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">-->
              <a class="dropdown-item" href=<%= users_path %>>All Users</a>
              <a class="dropdown-item" href=<%= activities_path %>>All Activities</a>
              <a class="dropdown-item" href=<%= activity_participants_path %>>All Activity Participants</a>
            </div>
          </li>
        <% end %>

      <% else %>
        <li class="nav-item">
          <!-- 'data-backdrop': true, 'data-keyboard': true -->
          <%= link_to "Login", login_path, {remote: true, 'data-toggle': "modal", 'data-target': "#login", class: "nav-link"}%>
        </li>
      <% end %>

<!--      <li class="nav-item">-->
        <%#= request.location.city %>
<!--      </li>-->
    </ul>
  </div>
</nav>