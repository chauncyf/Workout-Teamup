refreshPosts()
if ($('#posterModal').is(":not(':hidden')")) {
    posterModalRefresh()
    new PNotify({
        title: '<i class="fas fa-check-circle"></i> Comment Succeed',
        type: 'success',
        delay: 2000
    });
}
